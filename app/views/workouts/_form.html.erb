<%= form_with(model: workout, local: true) do |form| %>
  <% if workout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workout.errors.count, "error") %> prohibited this workout from being saved:</h2>

      <ul>
      <% workout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :workout_name %>
  </div>

  <div class="field">
    <%= form.label :comments_state %>
    <%= form.check_box :comments_state, id: :workout_comments_state %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :workout_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
